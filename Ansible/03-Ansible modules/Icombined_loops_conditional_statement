---
- name: Install packages on multiple hosts combined loops
  hosts: all
  become: true  # Run tasks with elevated privileges

  vars:
    package_cache_update:
      - python3-apt

    packages_to_install:
      - name: nginx
        state: present
      - name: apache2  # Correct package name for Apache
        state: present
